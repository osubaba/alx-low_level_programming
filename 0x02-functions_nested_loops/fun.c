#include <stdio.h>
/**
 * main - main block
 * Dicription: calling function
 * Return: returns (0)
 */
{
	void wife(char name[]);
        printf("Hello %s\n", name);
}
int main(void)
{
	wife(delphine);
	return (0);
}
